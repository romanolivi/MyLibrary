<h1>Users List</h1>

<h3>Click on a user to see their profile!</h3>

<ul>
<% @users.each do |u| %> 
    <li><%= link_to u.username, user_path(u) %> - <%= u.reviews.count %> reviews</li>
    <br>
<% end %> 
</ul>
